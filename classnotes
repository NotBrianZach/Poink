$hasedpwd=password_hash(password,PASSWORD_BCRYPT);
password_verify(entered password,hash from database)
#use post method to transmit password
<input type="password"

if (!isset($_SERVER['HTTPS']) || $_SERVER['HTTPS'] !== "on") {
    header('Location: https://' . $_SERVER['SERVER_NAME'] . $_SERVER['REQUEST_URI']);
    exit 0;
}
#this works only on apache, some other server configs might break.
